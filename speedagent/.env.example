# Copy to .env and fill in. .env is gitignored.

# Same INGEST_TOKEN as the Worker secret + the prober.
INGEST_TOKEN=replace-with-the-same-long-random-token

# Optional overrides:
# INGEST_URL=https://status.aswincloud.com/api/speedtest
# INTERVAL_SECONDS=900
